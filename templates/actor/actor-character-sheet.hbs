<form class="{{cssClass}} {{actor.type}} flexcol" autocomplete="off">

  {{!-- Sheet Header --}}
  <header class="sheet-header">
    <img class="profile-img" src="{{actor.img}}" data-edit="img" title="{{actor.name}}" height="100" width="100"/>
    <div class="header-fields">
      <div class="resources grid grid-3col">
        <div class="resource flex-group-center grid-span-2">
          <h1 class="charname"><input name="name" type="text" value="{{actor.name}}" placeholder="{{ localize 'LODGES.name' }}"/></h1>
        </div>
        <div class="resource flex-group-center">
          <label for="system.details.archetype" class="resource-label">{{ localize 'LODGES.archetype' }}</label>
          <div class="resource-content flexrow flex-center flex-between">
            <input type="text" name="system.details.archetype" value="{{system.details.archetype}}" data-dtype="String"/>
          </div>
        </div>
      </div>

      {{!-- The grid classes are defined in scss/global/_grid.scss. To use,
      use both the "grid" and "grid-Ncol" class where "N" can be any number
      from 1 to 12 and will create that number of columns.  --}}
      <div class="resources grid grid-5col">

        {{!-- "flex-group-center" is also defined in the _grid.scss file
        and it will add a small amount of padding, a border, and will
        center all of its child elements content and text. --}}
        <div class="resource flex-group-center">
          <label for="system.aspects.sociability.value" class="resource-label">{{ localize 'LODGES.sociability' }}</label>
          <div class="resource-content flexrow flex-center flex-between">
          <input type="text" name="system.aspects.sociability.value" value="{{system.aspects.sociability.value}}" data-dtype="Number"/>
          </div>
        </div>

        <div class="resource flex-group-center">
          <label for="system.aspects.combativity.value" class="resource-label">{{ localize 'LODGES.combativity' }}</label>
          <div class="resource-content flexrow flex-center flex-between">
          <input type="text" name="system.aspects.combativity.value" value="{{system.aspects.combativity.value}}" data-dtype="Number"/>
          </div>
        </div>

        <div class="resource flex-group-center">
          <label for="system.aspects.curiosity.value" class="resource-label">{{ localize 'LODGES.curiosity' }}</label>
          <div class="resource-content flexrow flex-center flex-between">
          <input type="text" name="system.aspects.curiosity.value" value="{{system.aspects.curiosity.value}}" data-dtype="Number"/>
          </div>
        </div>

        <div class="resource flex-group-center">
          <label for="system.aspects.ingenuity.value" class="resource-label">{{ localize 'LODGES.ingenuity' }}</label>
          <div class="resource-content flexrow flex-center flex-between">
          <input type="text" name="system.aspects.ingenuity.value" value="{{system.aspects.ingenuity.value}}" data-dtype="Number"/>
          </div>
        </div>

        <div class="resource flex-group-center">
          <label for="system.aspects.occult.value" class="resource-label">{{ localize 'LODGES.occult' }}</label>
          <div class="resource-content flexrow flex-center flex-between">
          <input type="text" name="system.aspects.occult.value" value="{{system.aspects.occult.value}}" data-dtype="Number"/>
          </div>
        </div>

      </div>

      <div class="resources grid grid-5col">

        {{!-- "flex-group-center" is also defined in the _grid.scss file
        and it will add a small amount of padding, a border, and will
        center all of its child elements content and text. --}}
        <div class="resource flex-group-center">
          <label for="system.aspects.sociability.quirk" class="resource-label">{{ localize 'LODGES.quirk' }}</label>
          <div class="resource-content flexrow flex-center flex-between">
          <input type="text" name="system.aspects.sociability.quirk" value="{{system.aspects.sociability.quirk}}" data-dtype="String"/>
          </div>
        </div>

        <div class="resource flex-group-center">
          <label for="system.aspects.combativity.quirk" class="resource-label">{{ localize 'LODGES.quirk' }}</label>
          <div class="resource-content flexrow flex-center flex-between">
          <input type="text" name="system.aspects.combativity.quirk" value="{{system.aspects.combativity.quirk}}" data-dtype="String"/>
          </div>
        </div>

        <div class="resource flex-group-center">
          <label for="system.aspects.curiosity.quirk" class="resource-label">{{ localize 'LODGES.quirk' }}</label>
          <div class="resource-content flexrow flex-center flex-between">
          <input type="text" name="system.aspects.curiosity.quirk" value="{{system.aspects.curiosity.quirk}}" data-dtype="String"/>
          </div>
        </div>

        <div class="resource flex-group-center">
          <label for="system.aspects.ingenuity.quirk" class="resource-label">{{ localize 'LODGES.quirk' }}</label>
          <div class="resource-content flexrow flex-center flex-between">
          <input type="text" name="system.aspects.ingenuity.quirk" value="{{system.aspects.ingenuity.quirk}}" data-dtype="String"/>
          </div>
        </div>

        <div class="resource flex-group-center">
          <label for="system.aspects.occult.contract" class="resource-label">{{ localize 'LODGES.contract' }}</label>
          <div class="resource-content flexrow flex-center flex-between">
          <input type="text" name="system.aspects.occult.contract" value="{{system.aspects.occult.contract}}" data-dtype="String"/>
          </div>
        </div>

      </div>

      <div class="resources grid grid-3col">

        <div class="resource flex-group-center">
          <label for="system.details.role" class="resource-label">{{ localize 'LODGES.role' }}</label>
          <div class="resource-content flexrow flex-center flex-between">
          <input type="text" name="system.details.role" value="{{system.details.role}}" data-dtype="String"/>
          </div>
        </div>

        <div class="resource flex-group-center">
          <label for="system.details.occupation" class="resource-label">{{ localize 'LODGES.secular_occupation' }}</label>
          <div class="resource-content flexrow flex-center flex-between">
          <input type="text" name="system.details.occupation" value="{{system.details.occupation}}" data-dtype="String"/>
          </div>
        </div>

        <div class="resource flex-group-center">
          <label for="system.details.era" class="resource-label">{{ localize 'LODGES.era' }}</label>
          <div class="resource-content flexrow flex-center flex-between">
          <input type="text" name="system.details.era" value="{{system.details.era}}" data-dtype="String"/>
          </div>
        </div>

      </div>

    </div>
  </header>

  {{!-- Sheet Tab Navigation --}}
  <nav class="sheet-tabs tabs" data-group="primary">
    {{!-- Default tab is specified in actor-sheet.mjs --}}
    <a class="item" data-tab="description">{{ localize 'LODGES.description' }}</a>
    <a class="item" data-tab="artefacts">{{ localize 'LODGES.artefacts' }}</a>
    <a class="item" data-tab="items">{{ localize 'LODGES.items' }}</a>
    <a class="item" data-tab="features">{{ localize 'LODGES.features' }}</a>
    <a class="item" data-tab="effects">{{ localize 'LODGES.effects' }}</a>
  </nav>

  {{!-- Sheet Body --}}
  <section class="sheet-body">

    {{!-- Description Tab --}}
    <div class="tab description" data-group="primary" data-tab="description">
      {{editor system.description target="system.description" rollData=rollData button=true owner=owner editable=editable}}
    </div>

    {{!-- Owned Artefacts Tab --}}
    <div class="tab artefacts" data-group="primary" data-tab="artefacts">
      {{> "systems/lodges/templates/actor/parts/actor-artefacts.hbs"}}
    </div>

    {{!-- Owned Items Tab --}}
    <div class="tab items" data-group="primary" data-tab="items">
      {{> "systems/lodges/templates/actor/parts/actor-items.hbs"}}
    </div>

    {{!-- Owned Features Tab --}}
    <div class="tab features" data-group="primary" data-tab="features">
      {{> "systems/lodges/templates/actor/parts/actor-features.hbs"}}
    </div>

    {{!-- Active Effects Tab --}}
    <div class="tab effects flexcol" data-group="primary" data-tab="effects">
      {{> "systems/lodges/templates/actor/parts/actor-effects.hbs"}}
    </div>

  </section>
</form>

